# Prevent platform-specific binaries from being installed
target_platform=linux
target_arch=x64
target_libc=glibc

# Use exact versions to prevent version conflicts
save-exact=true

# Skip optional dependencies that might cause platform issues
optional=false

# Ensure compatibility with Vercel's build environment
engine-strict=true
